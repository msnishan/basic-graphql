type Query {
    posts: [Post]
    postById(id: ID): Post
}

type Post {
    id: ID!
    text: String!
    author: Author!
    comments: [Comment]
}

type Author {
    id: ID!
    name: String!
}

type Comment {
    id: ID!
    text: String!
    author: Author!
}